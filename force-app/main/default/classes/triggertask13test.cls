@isTest
public class triggertask13test {
@isTest    
public static void insertcontact() {
 Contact con = new Contact(LastName = 'Testing DP' );
 Test.startTest();
 insert con;
Test.stopTest();       
 }
}